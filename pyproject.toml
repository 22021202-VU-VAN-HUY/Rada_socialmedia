[project]
name = "talent-radar"
version = "0.1.0"
description = "VSF-first social listening MVP dashboard prototype"
requires-python = ">=3.11"
dependencies = [
  "streamlit>=1.36",
  "pandas>=2.2",
  "requests>=2.31",
]

[tool.ruff]
line-length = 100
