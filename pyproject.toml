[project]
name = "talent-radar"
version = "0.1.0"
description = "Local background social collection dashboard"
requires-python = ">=3.11"
dependencies = [
  "alembic>=1.14",
  "beautifulsoup4>=4.12",
  "fastapi>=0.115",
  "httpx>=0.27",
  "pydantic>=2.8",
  "pydantic-settings>=2.4",
  "playwright>=1.49",
  "pywinauto>=0.6.9",
  "psycopg[binary]>=3.2",
  "pyyaml>=6.0",
  "sqlalchemy>=2.0",
  "uvicorn[standard]>=0.30",
]

[tool.ruff]
line-length = 100

[project.optional-dependencies]
dev = [
  "pytest>=8.2",
]
